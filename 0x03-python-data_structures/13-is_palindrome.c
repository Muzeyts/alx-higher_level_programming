#include "lists.h"

/**
 * is_palindrome - this checks if a singly linked list is a palindrome
 * @head: which pointer to head of list
 * Return: returns 1 if it is a palndrome, and 0 if not
 */
int is_palindrome(listint_t **head)
{

}
